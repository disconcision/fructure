#lang setup/infotab

(define name "containment-patterns")
(define blurb '("Pattern matcher for capturing contexts"))
(define primary-file "main.rkt")
(define scribblings '(("containment-patterns.scrbl" ())))
(define version "1")

#;(define categories '(misc))
#;(define release-notes '((p "init")))
#;(define repositories '("4.x"))
#;(define required-core-version "5.0")


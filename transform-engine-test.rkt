#lang racket

(require racket/gui/base)
(require fancy-app)


; -----------
; notes for tommorow:
; forget gui for moment, just do text output
; manually wrap (single) selector into code tree
; make and manually test pattern matchers for selector movement
; make those work recursively
; make function to remove selector from tree
; make function to insert selector at provided position